require 'spec_helper'
require 'shared_contexts'

<% @classes_and_defines.each do | value |  %>
    <% types = value[:types]  %>
    <% types.each do |type| %>
    <% tname = type[:name] %>

describe '<%= tname %>' do
        #include_context :some_context
        let(:facts) do
            {}
        end
        let(:params) do
            {}
        end

        it { should compile }
        <% end %>
    <% end %>

end


